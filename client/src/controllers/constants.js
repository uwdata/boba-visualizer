const UNC_TYPE = {
  AGG: 'Aggregated',
  PDF: 'PDFs',
  CDF: 'CDFs'
}

const COLOR_TYPE = {
  P: 'P-value',
  SIGN: 'Sign',
  FIT: 'Model Fit'
}

const SCHEMA = {
  POINT: 'point_estimate',
  FIT: 'fit',
  P: 'p_value',
  UNC: 'uncertainty'
}

const sign = 0

export {UNC_TYPE, COLOR_TYPE, SCHEMA, sign}
